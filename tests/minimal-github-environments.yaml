repositories:
  - repo: "stratus-tf-aca-gh-vending"
    environments:
      # Minimal test environment - only required fields
      - name: "minimal-test"
        container_environment: "default"
        variables:
          TEST_TYPE: "minimal" 